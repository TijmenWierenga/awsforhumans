<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="theme-color" content="#794ACF">
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
    <title>AWS For Humans{% if block('title') is not empty %} - {% endif %}{% block title %}{% endblock %}</title>
</head>
<body>
    <div id="app" class="flex flex-col min-h-full">
        {% include 'partials/navigation.html.twig' %}
        <div class="container m-auto flex flex-grow p-4 lg:pt-8">
            <div class="flex flex-col lg:flex-row">
                <div class="flex mb-4 lg:w-1/5">
                    {% block sidebar_left %}{% endblock %}
                </div>
                <div class="flex flex-col lg:w-3/5 lg:px-8">
                    {% block content %}{% endblock %}
                </div>
                <div class="flex mb-4 lg:w-1/5">
                    {% block sidebar_right %}{% endblock %}
                </div>
            </div>
        </div>
    </div>
    <script src="{{ asset('build/app.js') }}"></script>
</body>
</html>